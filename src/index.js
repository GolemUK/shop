import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
 // Якщо у вас є стилі

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);